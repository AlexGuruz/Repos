# Validate in playground / SDL.
query DiscoverFindProductCategories {
  findProductCategories(first: 20) {
    count
    pageInfo {
      hasNextPage
      endCursor
    }
    edges {
      node {
        objectId
        name
      }
    }
  }
}
