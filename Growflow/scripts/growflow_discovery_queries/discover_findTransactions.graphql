# Minimal root probe — expand node fields once schema/samples exist.
query DiscoverFindTransactions {
  findTransactions(first: 5) {
    count
    pageInfo {
      hasNextPage
      endCursor
    }
    edges {
      node {
        objectId
      }
    }
  }
}
