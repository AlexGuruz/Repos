# Workflow: Company-first Triage

Sequence:
Ingest → Mover → **Triage** → (Pending|Matched) → Matcher → Sheets Poster → Replay

Progress (per company, per batch):
- triage.matched, triage.unmatched, pending.posted, matched.posted, pending.replayed
