# Archive

This directory is reserved for any Kylo-specific or project-specific files that might be useful as reference material.

**By default, this directory is empty.**

If you need to reference the original REMODEL repository for specific implementations, see `D:\REMODEL` (the original source repository remains intact).
