# Dry Run — 2026-07-15 (post-qual hardening)

**Verdict:** `READY FOR CONTROLLED INVESTOR DEMO` (dry-run path green)

## What was exercised

| Step | Result | Evidence |
|------|--------|----------|
| Clean stop + full-mode restart (`CC_LIGHT_MODE=0`) | PASS | CC `GET /api/health` 200 |
| WA tunnel SoT | PASS | `curl http://127.0.0.1:8765/health` 200 |
| Approve + execute (`cc_investor_demo_ping`) | PASS | `approval-124` → `ACT-approval-124` |
| Deny (no execute) | PASS | `approval-125` denied |
| Always Approve (permanent **before** resolve) | PASS | `PAR-B140C959` from `approval-130` |
| Matching re-propose auto-path | PASS | `approval_required=false`, `auto_approved` |
| Non-match still pending | PASS | `DRY_NONMATCH_MARKER` |
| Remove permanent rule | PASS | `DELETE /api/approvals/permanent/PAR-B140C959` |
| After delete → pending again | PASS | `approval-133` pending |

Summary JSON: `evidence/dry_stage456_summary.json`

## Hardening shipped this session

1. **Durable WA launcher** — `ai-lab/scripts/wa_serve_asyncio.py`, `run_wa_serve.bat`, `ensure_worker_assistant_asyncio.ps1` (deployed to worker-node `C:\worker\logs\worker_assistant\` + `C:\worker\worker_ai\scripts\`).
2. **Workers health budget** — interactive budget 5s; per-service cap 3s (was 1.2s). WA no longer false-negative when curl is 200.
   - Before: `dry_workers_health_before.json` (WA timeout @ 1200ms)
   - After: `dry_workers_health_final.json` (WA ok ~1.4–2.6s)

## Preflight for camera

- Use **`curl http://127.0.0.1:8765/health`** as WA SoT (not only `/api/workers/health`).
- **Always Approve order:** `POST /api/approvals/permanent` with `approval_id` **while still pending**, then resolve approved (matches UI).
- Clear pending backlog noise before demo (~47 legacy items at dry-run time); new ping cards are isolated.
- Tool on camera: **`cc_investor_demo_ping`** only for Approve/Deny/Always.
