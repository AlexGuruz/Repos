# Retry pass — 2026-07-15 ~14:33 local

Branch: `feature/kylo-bank-cash-dual-rules` @ `921a5dd`  
Stack: CC :8000 + frontend :5173 up; WA restarted via `run_wa_serve.bat`

## Worker

- Direct `GET http://127.0.0.1:8765/health` → **200** `ok`
- `/api/workers/health` still often marks worker_assistant **timeout** (tight 1200ms budget) even when curl succeeds — treat **curl** as SoT for demo preflight

## Stages 4–6 (`cc_investor_demo_ping`)

| Step | ID | Result |
|------|-----|--------|
| Approve | approval-118 | execute success=true exit=0; publish_ms=0 total_ms=31 |
| Deny | approval-119 | executed=false |
| Always + PAR | approval-120 → PAR-68D535AA | execute success |
| Matching re-propose | | approval_required=false auto_approved |
| Nonmatch | approval-122 | pending then denied |
| Delete PAR | | ok; matching again pending (approval-123) |

Evidence: `evidence/retry_propose_*.json`, `retry_resolve_*.json`, `retry_stage456_summary.json`

## Tunnel fairness

`evidence/retry_tunnel_fairness.json`: control metrics **7/7** OK while bulk `index_repo` submitted (lane bulk submitted≥1). Bulk body still 503 governance read-only (expected without WA AI_LAB_*). Interactive invoke ok=true.

## Verdict

Hard gates still hold for controlled demo.

READY FOR CONTROLLED INVESTOR DEMO
