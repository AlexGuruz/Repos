Bundled Gmail API client for **monorepo / minimal checkout** use. Same module layout as `Ai/Email-Inbox-Agent---Doo-Made` (`app.gmail_client`). Prefer the full `Ai/...` tree when present; otherwise `backfill` injects this path.

Place `credentials.json` / `token.json` here, or use `GOOGLE_CREDENTIALS_FILE` and `GOOGLE_TOKEN_FILE`.
